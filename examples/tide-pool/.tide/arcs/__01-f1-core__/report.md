# report — f1-core
contract: f1-core
accepted: yes

Built core Tide Pool in single self-contained index.html (canvas, no build). Cozy teal pool with drifting caustics + vignette. pointerdown spawns an expanding ripple + a popping plankton that drifts/bobs, and increments a large HUD plankton counter by exactly +1. Live particles capped at 240 (counter keeps climbing); all state in plain JS, no localStorage (persistence deferred). Wrote output/notes.md, output/index.html, proof screenshot, and proposed cannon delta in delta.md.
