# 01-f1-core

goal: Single-file index.html idle/clicker base — click the tide pool to spawn plankton + bump a counter with ripple feedback.
status: done
cannon-rev: a3da426ec1c0
# supersedes: <slug of the arc this one replaces — optional; alias: prev:>

## input
Empty workspace — first arc; builds the Tide Pool game from scratch.

## output → pointers
- index.html — the built single-file game (canvas pool + HUD + click→spawn→ripple + window.TidePool store).
- output/notes.md — build + downstream-arc notes; output/result.txt — pointer to the game file.
