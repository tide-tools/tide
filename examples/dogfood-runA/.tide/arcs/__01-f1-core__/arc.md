# 01-f1-core

goal: Core Tide Pool click+spawn loop — click the pool to gather plankton (counter +1, sprite spawn, ripple feedback) in a single self-contained index.html.
status: done
cannon-rev: 3910c43c2667
# supersedes: <slug of the arc this one replaces — optional; alias: prev:>

## input
Empty workspace — first feature, establishes the single-file game substrate.

## output → pointers
- index.html — the whole game (core click+spawn+count loop).
- output/notes.md — build notes + criteria mapping + verification.
