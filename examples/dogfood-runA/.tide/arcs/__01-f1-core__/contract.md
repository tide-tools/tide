# contract — f1-core

slug: f1-core
goal: Clicking the tide pool spawns a plankton and increments a visible plankton counter, with a small visual feedback animation. Single self-contained index.html (HTML+CSS+JS), Canvas or DOM, opens directly in a browser with no build step.
criteria: 1) index.html exists and opens standalone in a browser. 2) A clearly clickable tide-pool area is present. 3) Each click increments a visible numeric plankton counter by exactly 1. 4) Each click spawns a visible plankton element/sprite inside the pool. 5) Each click produces a brief visual feedback (ripple/flash/scale). 6) No JS console errors on load or on click. 7) No external network/CDN dependencies.
project: ~/projects/tide/examples/dogfood-runA
state: close
sign: orchestrator @ 2026-06-25
# supersedes: <slug of the contract this one pivots from — optional; alias: prev:>
cannon-rev: ce43a1d5ffd5

## IS → TO-BE
<where it is now → where this contract takes it>

## where we are
<current step / bottleneck>
