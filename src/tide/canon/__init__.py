"""tide.canon — durable truth home (CANON.md living-IS doc).

Spelling: canon = body of truth (correct spelling); the old "cannon" (weapon)
was a typo corrected in v1.0.2. Planned modules (later units):
  store.py   — canon/ init (CANON.md + config), read / scan (U2)
  rev.py     — canon-rev = short sha256 of CANON.md ONLY (the truth) (U2)
  merge.py   — merge_delta(): append delta under '## Canon journal' (U2)
  board.py   — canon status: scan per-arc homes, group by 5-state (U8)

canon-merge is the single serialization point and is ORCHESTRATOR-ONLY.
"""
